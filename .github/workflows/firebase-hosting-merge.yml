# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install
        run: 'npm ci'

      - name: Release
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FIREBASE_SERVICE_ACCOUNT_BVR_APP_4FFF0: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BVR_APP_4FFF0 }}
          FIREBASE_SERVICE_ACCOUNT_BVR_APP_WEBSITE: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BVR_APP_WEBSITE }}
        run: 'npm run ci:release'

#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BVR_APP_4FFF0 }}'
#          channelId: live
#          projectId: bvr-app-4fff0
#          entryPoint: 'dist/apps/app'
#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BVR_APP_WEBSITE }}'
#          channelId: live
#          projectId: bvr-app-website
#          entryPoint: 'dist/apps/app-website'
